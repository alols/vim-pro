Automatically updates tags when saving and provides a project-wide grep
command.

TODO:
Shorter commands?
Documentation
Add settings for ctags command line
Make it easier to add files
Make it easier to open a project
Tab completion when loading project/adding files?
Handle if ctags executable is missing
Add cscope support
